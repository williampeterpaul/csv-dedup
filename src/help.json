{
  "name": "csv-dedup",
  "about": "Merge, deduplicate, and combine CSV files",
  "commands": {
    "outer": {
      "about": "Full outer join — all rows from all files, merged on keys",
      "usage": "csv-dedup outer <file1.csv> [file2.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup outer a.csv b.csv -k domain,name",
        "csv-dedup outer a.csv b.csv -k id -c id,name,email -o merged.csv"
      ]
    },
    "inner": {
      "about": "Inner join — only rows whose keys appear in all input files",
      "usage": "csv-dedup inner <file1.csv> [file2.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup inner a.csv b.csv -k domain,name",
        "csv-dedup inner a.csv b.csv c.csv -k id -o common.csv"
      ]
    },
    "left": {
      "about": "Left join — source rows enriched with data from other files",
      "usage": "csv-dedup left <source.csv> <other.csv> [other2.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup left source.csv extra.csv -k id",
        "csv-dedup left main.csv enrichment.csv -k domain,name -o enriched.csv"
      ]
    },
    "exclude": {
      "about": "Keep only source rows whose keys don't appear in any other file",
      "usage": "csv-dedup exclude <source.csv> <other.csv> [other2.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup exclude source.csv blocklist.csv -k email",
        "csv-dedup exclude main.csv dupes1.csv dupes2.csv -k domain,name"
      ]
    },
    "dedup": {
      "about": "Deduplicate rows within a CSV, keeping first occurrence per key",
      "usage": "csv-dedup dedup <file.csv> [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup dedup messy.csv -k email",
        "csv-dedup dedup contacts.csv -k first,last -o clean.csv"
      ]
    },
    "unique": {
      "about": "Keep only rows whose keys appear in exactly one file",
      "usage": "csv-dedup unique <file1.csv> <file2.csv> [file3.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup unique a.csv b.csv -k email",
        "csv-dedup unique list1.csv list2.csv list3.csv -k id -o only-in-one.csv"
      ]
    },
    "dupes": {
      "about": "Keep only rows whose keys appear in more than one file",
      "usage": "csv-dedup dupes <file1.csv> <file2.csv> [file3.csv ...] [options]",
      "options": [
        { "flag": "-k, --keys <cols>", "about": "Comma-separated key column(s) for matching rows (required)" },
        { "flag": "-c, --columns <cols>", "about": "Comma-separated columns to include in output (default: all)" },
        { "flag": "-o, --output <path>", "about": "Output file path (default: <first-file>.out.csv)" },
        { "flag": "-h, --help", "about": "Show this help" }
      ],
      "examples": [
        "csv-dedup dupes a.csv b.csv -k email",
        "csv-dedup dupes list1.csv list2.csv -k domain,name -o overlaps.csv"
      ]
    }
  }
}
